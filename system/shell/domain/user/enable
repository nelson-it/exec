#!/bin/bash

find_script domain/detail read.sh

domainconf=`get_data "$getdomain"`
get_domain "$domainconf"

if [ "$typ" = "" ]; then
    exit 0
fi

. $(dirname $BASH_SOURCE)/validuser
. $(dirname $BASH_SOURCE)/../restart/$typ

exit 0
